2026-08-02 09:12 — Serve the freelancer contract app at /contract via a Next rewrite to the separate taxchat Vercel project, rather than merging its code into this site — the two have different auth, database and release cadence, and a rewrite keeps one domain without coupling them.
2026-08-02 09:12 — Exclude /contract from this site's headers() rule — a proxied app carries its own security headers, and this site's Permissions-Policy `camera=()` would have blocked the contract app's ID-card capture (it sets `camera=(self)`). Verified the exclusion regex against Next's own path matcher: `/` and `/contracts` still get headers, only `/contract` and below are exempt.
